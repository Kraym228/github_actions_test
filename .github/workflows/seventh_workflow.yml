name: Seventh Workflow 

on: 
    schedule:
      - cron: "*/5 * * * *"  # https://crontab.cronhub.io/
   
jobs:
    run-first-command: #name
        runs-on: ubuntu-latest #os 
        steps:
            - name: Event Vars 
              run: |
                echo $GITHUB_SHA
                echo $GITHUB_REF
