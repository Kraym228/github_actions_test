name: Seventh Workflow 

on: 
    schedule:
      - cron: "*/5 * * * *"  # https://crontab.cronhub.io/
   
jobs:
    run-first-command: #name
        runs-on: ubuntu-latest #os 
        steps:
            - name: Event Vars 
              run: |
                echo $GITHUB_SHA
                echo $GITHUB_REF
#    call-external-workflow:
#      uses: KRAYM228/test/.github/workflows/workflow_shablon #id указываем репозитория который нужен( логин, название KRAYM228/Название репозитория).Внешний репозттории
#      secrets:
#        PASSWORD: ${{secrets.PASSWORD}}