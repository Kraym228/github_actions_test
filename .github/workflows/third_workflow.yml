name: Thrid Workflow 

on: push # event 

jobs:
    run-first-command: #name
        runs-on: ubuntu-latest #os 
        steps:
            - name: Display some Text 
              run: echo "Some Text" 
            - name: Command in Miltiline 
              run: |
                pwd
                ls -a  