name: Fifth Workflow 

on: 
    issues:
        types: [edited, deleted] # event 
    pull_request:
        types: [assigned, closed]

jobs:
    run-first-command: #name
        runs-on: ubuntu-latest #os 
        steps:
            - name: Event Vars 
              run: |
                echo $GITHUB_SHA
                echo $GITHUB_REF
